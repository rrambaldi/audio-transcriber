name: srt-ov
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - optimum-intel[openvino]
      - transformers
      - imageio-ffmpeg   # ffmpeg incluso: nessuna installazione esterna
      - numpy
      - pyannote.audio   # solo per --diarize ("chi dice cosa"), gira su CPU
